var a = require('./')

a.auth.publicKey = 'wtfismygs'
a.auth.privateKey = 'yrkhc8xH'

a._get('/realm/status', {region:'us', _query:{}}, function(e,b,r) {
    console.log(r.req.getHeader('Authorization'))
})

a.character({region: 'us', realm: 'shadowmoon', id: 'dargonaut'}, function(e,b,r) {
    console.log(r.req.getHeader('Authorization'))
})
